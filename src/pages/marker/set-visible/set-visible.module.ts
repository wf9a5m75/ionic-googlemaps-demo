import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SetVisiblePage } from './set-visible';

@NgModule({
  declarations: [
    SetVisiblePage,
  ],
  imports: [
    IonicPageModule.forChild(SetVisiblePage),
  ],
})
export class SetVisiblePageModule {}
