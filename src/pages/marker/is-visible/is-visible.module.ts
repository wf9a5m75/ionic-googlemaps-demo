import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { IsVisiblePage } from './is-visible';

@NgModule({
  declarations: [
    IsVisiblePage,
  ],
  imports: [
    IonicPageModule.forChild(IsVisiblePage),
  ],
})
export class IsVisiblePageModule {}
