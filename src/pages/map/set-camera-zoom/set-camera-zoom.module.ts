import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SetCameraZoomPage } from './set-camera-zoom';

@NgModule({
  declarations: [
    SetCameraZoomPage,
  ],
  imports: [
    IonicPageModule.forChild(SetCameraZoomPage),
  ],
})
export class SetCameraZoomPageModule {}
