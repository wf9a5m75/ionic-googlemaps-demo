import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MoveCameraZoomInPage } from './move-camera-zoom-in';

@NgModule({
  declarations: [
    MoveCameraZoomInPage,
  ],
  imports: [
    IonicPageModule.forChild(MoveCameraZoomInPage),
  ],
})
export class MoveCameraZoomInPageModule {}
