import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AnimateCameraZoomInPage } from './animate-camera-zoom-in';

@NgModule({
  declarations: [
    AnimateCameraZoomInPage,
  ],
  imports: [
    IonicPageModule.forChild(AnimateCameraZoomInPage),
  ],
})
export class AnimateCameraZoomInPageModule {}
